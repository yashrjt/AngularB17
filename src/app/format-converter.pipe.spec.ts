import { FormatConverterPipe } from './format-converter.pipe';

describe('FormatConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
